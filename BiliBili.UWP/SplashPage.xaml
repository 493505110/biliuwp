<Page
    x:Class="BiliBili.UWP.SplashPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BiliBili.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 
    
    mc:Ignorable="d">

    <Grid Background="{ThemeResource Bili-Background}">
        <Grid>
            <StackPanel VerticalAlignment="Center">
                <Image x:Name="load_img" MaxWidth="350">
                    <Image.Source>
                        <!--原图 1080x1920 RGBA，全尺寸解码约 7.9MB、耗时偏长，会出现
                            「加载中... 已显示、2233 立绘还没出来」。限制解码宽度即可显著加快
                            （只显示到 350 DIP，700 已足够覆盖高 DPI）-->
                        <BitmapImage UriSource="ms-appx:///Assets/Splash.png" DecodePixelWidth="700" />
                    </Image.Source>
                </Image>
                <!--<Image Source="ms-appx:///HappyBirthDay.png"  Width="300"></Image>-->
            </StackPanel>
            <StackPanel VerticalAlignment="Bottom" Margin="0 30">
                <ProgressRing Width="50" Height="50" IsActive="True" Margin="0 5" Foreground="Gray"></ProgressRing>
                <TextBlock HorizontalAlignment="Center"  Foreground="Gray" Text="加载中..."></TextBlock>
            </StackPanel>
        </Grid>
        
    </Grid>
</Page>
